# Where the ronsel API under test listens. scripts/e2e.js starts it on an
# empty scratch context, which ronsel furnishes with its bundled examples on
# first start -- so the flows here can count on those.
BASE_URL=http://localhost:3001
# How long waitForRun waits for a flow started through the API to finish.
RUN_TIMEOUT_MS=60000
