# The command line under test, relative to the repository root. dist/ is what
# `npm run build` writes and what the published package ships.
CLI=dist/cli.js
# How long one invocation may take before it is killed and the step fails.
TIMEOUT_MS=60000
