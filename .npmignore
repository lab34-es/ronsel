# The published contents are declared by the "files" allowlist in package.json
# (dist/ plus the readme and licence), which takes precedence over this file.
# Kept only as a backstop for anything an npm version might still consider.
node_modules/**
src/**
tests/**
frontend/
.github/
.vscode/
.claude/
coverage/
tester-darwin-arm64/**
flows-tester-darwin-arm64/**
tester-win32-x64/**
